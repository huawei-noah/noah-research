# Recommended: use ``uv sync`` to install the locked full environment, including the
# in-repo DeepCraft subpackages, the official MLE-bench Git version, and the test/model
# dependencies. This file only keeps the lightweight runtime dependency list.

python-dotenv>=1.0
pydantic>=2.0
omegaconf>=2.3
pyyaml>=6.0
psutil>=5.9
aiofiles>=23.0
rich>=13.0
click>=8.1
dataclasses-json>=0.6.0
openai>=1.0
pydicom>=2.4
