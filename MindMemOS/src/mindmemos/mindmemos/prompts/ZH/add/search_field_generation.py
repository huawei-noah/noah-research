SEARCH_FIELD_GENERATION_PROMPT = """
你是搜索优化专家。为记忆实体生成多样化的检索字段，使其可以被各种自然语言查询找到。

## 实体信息
- 名称: {entity_name}
- 类型: {entity_type}
- 描述: {entity_description}
- 属性:
{entity_properties}

## 任务
生成最多 {max_fields} 个简短搜索短语（每个5-20个词），捕获该实体的不同方面。这些将被独立嵌入用于向量相似度搜索。

## 指南
1. **多样性**: 每个字段应捕获不同的方面或角度，避免冗余。
2. **查询导向**: 用用户自然搜索信息的方式编写短语。思考用户可能使用什么问题或关键词。
3. **全面覆盖**: 涵盖属性中提到的身份、活动、关系、偏好、事件和时间方面。
4. **自含完整**: 每个短语应独立有意义，不需要其他短语补充。
5. **包含实体名**: 每个字段至少提及一次实体名称，以便消歧。
6. **内容多时优先分组概括**: 当实体属性信息量大、无法在 {max_fields} 个字段内逐条覆盖时，将相关信息分组概括为更高层次的短语，尽可能覆盖全面。实在无法全部覆盖时，优先保留覆盖范围广、重要性高的内容（如核心身份、关键关系、重大事件），舍弃细枝末节。

## 示例
对于实体 "Caroline (person)"，属性涉及参加支持小组、绘画和养宠物：
```json
["Caroline定期参加LGBTQ支持小组活动",
 "Caroline的爱好是绘画，包括一幅亚拉冈的画作",
 "Caroline养了一只名叫Toby的狗",
 "Caroline在支持小组中受到跨性别故事的启发",
 "Caroline的情感健康和社交活动"]
```

## 输出格式
返回JSON字符串数组，每个字符串是一个检索字段。
```json
["字段1", "字段2", ...]
```

只输出JSON数组，不要额外文字。
"""

SEARCH_FIELD_UPDATE_PROMPT = """
你是搜索优化专家。根据新增信息更新记忆实体的检索字段。

## 实体：{entity_name}（{entity_type}）

## 当前检索字段
{current_fields}

## 新增信息
{new_information}

## 任务
返回更新后的检索短语列表，最多 {max_fields} 个。你可以：
- **保留**仍然相关的现有字段
- **更新**现有字段以融入新信息
- **替换**不太重要的字段，用新字段覆盖新增信息
- **添加**新字段（如果总数未满 {max_fields}）

## 规则
1. 总字段数不得超过 {max_fields}。如果已满，必须替换或合并才能添加新覆盖。
2. 每个字段5-20个词，提及实体名称，独立有意义。
3. 优先保留覆盖范围广、重要性高的字段（核心身份、关键关系、重大事件）。
4. 不要创建冗余字段——每个应覆盖不同方面。
5. 保留仍然准确代表实体的现有字段。只修改新信息要求变更的部分。

## 输出格式
返回JSON字符串数组（完整的更新后字段列表）。
```json
["字段1", "字段2", ...]
```

只输出JSON数组，不要额外文字。
"""

EPISODE_SEARCH_FIELD_AUGMENT_PROMPT = """
你是搜索优化专家。一个episode（对话片段）已经有了从结构化实体属性中提取的检索字段。你的任务是发现原始对话中存在但现有属性字段未覆盖到的事实，并为它们生成额外的检索短语。

## 原始对话
{episode_text}

## 已有检索字段（已覆盖的信息）
{existing_fields}

## 任务
生成最多 {augment_count} 条简短检索短语（每条5-20个词）。如果已有字段已经很好地覆盖了对话内容，可以生成少于 {augment_count} 条。

要求：
1. 每条短语必须捕获对话中存在但现有字段未覆盖到的事实
2. 关注：隐含事实、因果关系、具体细节（人名、地名、物品、日期）、情感背景、计划、建议，或属性提取遗漏的任何可检索信息
3. 使用陈述句，不要使用疑问句
4. 每条独立有意义，自含完整
5. 不要重复或改述已有字段中的信息

## 输出格式
返回1到 {augment_count} 个字符串的JSON数组。
```json
["短语1", "短语2", ...]
```

只输出JSON数组，不要额外文字。
"""
