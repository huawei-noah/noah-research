EPISODE_EDGE_PROMPT = """
你是记忆关系专家。请分析新创建的情景记忆与记忆库中已有情景记忆之间的关系。

## 新情景
名称: {new_episode_name}
描述: {new_episode_description}

## 候选情景
{candidate_episodes}

## 任务
对每个候选情景，判断是否应在新情景与该候选之间创建边（关系）。

## 建边标准 — 严格（必须全部满足）

只有当两个情景之间存在**具体的、可指名的事实桥梁**时才建边。必须满足以下至少一条：

1. **同一具体事件或计划**: 两个情景讨论的是完全相同的事件、旅行、预约、采购或计划（不是同一大类活动）。
   - 可以建边: "Andrew周六去Eagle Peak徒步" ↔ "Andrew为Eagle Peak徒步做准备"
   - 不可建边: "Andrew去徒步了" ↔ "Andrew去钓鱼了"（同属户外活动，但是不同事件）
   - 不可建边: "Andrew做了意大利面" ↔ "Andrew尝试攀岩"（同属爱好探索，但毫不相关）

2. **直接因果链**: 一个情景的结果被另一个情景明确提及或直接触发。
   - 可以建边: "Andrew在DoorDash失业" → "Andrew失业后决定开舞蹈工作室"
   - 不可建边: "Andrew失业了" → "Andrew开始学做菜"（时间上先后 ≠ 因果链）

3. **同一命名实体的状态变化**: 两个情景追踪同一个具体实体（人、宠物、地点、物品）的状态变迁。
   - 可以建边: "Andrew领养了Toby" ↔ "Toby的第一次兽医检查"
   - 可以建边: "Audrey搬进新公寓" ↔ "Audrey布置新公寓"
   - 不可建边: "Andrew聊了狗的话题" ↔ "Andrew聊了宠物话题"（同一话题 ≠ 同一实体状态变化）

## 常见错误 — 以下情况不应建边
- 两个情景提到同一个人但在做不同的事 → 不建边
- 两个情景属于同一大类话题（美食、狗、户外）→ 不建边
- 连续发生在同一天的不同对话 → 不建边
- "两者都涉及X分享经历" 或 "两者都描述X的休闲活动" → 不建边（太泛化）
- 相似的情感基调或对话风格 → 不建边

## relation 描述规则
`relation` 字段必须指明**具体的共享事实**，而不是类别概括。
- 正确: "两者都讨论了Andrew计划7月15日去Eagle Peak徒步"
- 正确: "情景A中Toby的领养导致了情景B中Toby的兽医检查"
- 错误: "两个情景都涉及Andrew讨论户外活动"
- 错误: "Andrew爱好探索的顺序发展"

## 输出格式
输出一个 JSON 数组。对每个应建立连接的候选，包含：
```json
[
    {
        "target_episode_name": "候选情景名称",
        "target_episode_id": "候选情景ID",
        "relation": "连接两个情景的具体共享事实"
    }
]
```

如果没有候选应被连接（大多数情况下这是正常的），输出空数组：`[]`

**拿不准时，不要建边。** 漏建一条边的代价很低（检索器有向量搜索兜底）；而一条噪声边会在多跳扩展中引入大量无关结果。

仅输出 JSON，不要额外文字。
"""
