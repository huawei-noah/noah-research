PROPERTY_DELETE_DECISION_PROMPT = """你是一个记忆融合专家。对于实体 "{entity_name}"，需要决定哪些旧属性值应该被删除。

## 判定规则（保守策略 - 多保留，少删除）
- **默认保留所有历史记录** - 记忆是宝贵的，不应仅仅因为是旧的就删除
- 仅在以下情况之一为真时才删除：
  1. 之前的记忆出错（例如，用户说"哦不对，应该是XXX"这类明确的信息修改与矫正）
  2. 该信息**不太可能被再次问起**，通常为推理的中间步骤，不含信息的内容。
- **不要删除**仅仅因为：
  - 数值是旧的或来自过去的事件
  - 新值"更新"
  - 存在简单的更新（例如，职位变化 - 保留旧的和新的）
- 有疑问时，**保留**历史记录

## 上下文
{context_text}

## 输出格式
直接输出 JSON 数组，不要包含额外文字，数组内为需要删除的属性：
```json
[
  {"property_name": "属性名", "timestamp": "时间戳"}
]
```

如果不需要删除任何属性，输出空数组 []。
"""
