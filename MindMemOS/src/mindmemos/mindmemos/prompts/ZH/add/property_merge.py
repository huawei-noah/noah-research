PROPERTY_MERGE_DECISION_PROMPT = """
你是记忆属性融合专家。判断新属性与已有属性之间的关系。

## 实体：{entity_name}（{entity_type}）

## 已有属性（记忆库中）
{existing_properties}

## 新属性（待添加）
{new_properties}

## 重要：默认行为
大多数情况下，两个列表都应该为空——已有属性保留，新属性原样添加。只有在有明确、合理的理由时才输出变更项。不要过度合并或过度删除。

**⚠️ 防信息丢失保护：**
在输出任何 delete 或 update 操作之前，必须验证：
1. **删除已有属性**：旧属性的每一条事实（名称、日期、地点、细节）必须在其他地方完整保留。如果旧属性包含任何新属性中没有的独特细节——保留它。
2. **删除新属性**：新属性的每一条事实必须已存在于某个已有属性中。如果新属性提到了任何已有属性中没有的细节（具体名称、日期、地点、数字）——不要删除。
3. **更新/合并**：合并后的值必须包含旧值和新值的所有事实。合并过程中不允许丢弃任何细节。
4. **不确定时**：不输出（保留两条）。冗余属性的代价可忽略；丢失事实的代价是永久的。

只有以下情况才应变更属性：
- **明确冗余**：新值的所有信息都已被某个已有属性完整包含（新值中的每一条事实都已出现在旧值中）
- **明确取代**：新事实明确使旧事实过时（如：计划已执行、状态已解决）
- **信息不完整**：属性存在未消解的指代、缺失的上下文或模糊的细节，另一条属性可以补全

以下情况不应变更属性：
- 两条属性描述不同事件/时间，即使话题相似（如：两次不同的远足→都保留）
- 一条属性包含另一条中没有的任何独特细节，即使部分重叠
- 不确定时——保留两条（不输出）

## 规则
对每个已有属性（p1, p2, ...）：
- **delete**：仅当旧值在事实上已过时，且有新属性明确取代它。旧信息必须完全被其他属性覆盖。
- **update**：仅当某个新属性为旧值补充了缺失的上下文（代词、意图、细节）。输出合并后的 `value`。
- *（不输出）*：保持不变。这是默认行为。

对每个新属性（n1, n2, ...）：
- **delete**：仅当新值中的每一条事实都已明确存在于某个已有属性中。不允许任何信息丢失。
- **update**：新值应合并到已有属性中。提供 `target`（哪个 p 项）和合并后的 `value`。
- *（不输出）*：原样添加。这是默认行为。

## 输出格式
```json
{{
  "existing": [],
  "new": []
}}
```

需要变更时（少见）：
```json
{{
  "existing": [
    {{"id": "p1", "op": "delete"}},
    {{"id": "p2", "op": "update", "value": "合并后的值"}}
  ],
  "new": [
    {{"id": "n1", "op": "delete"}},
    {{"id": "n2", "op": "update", "target": "p3", "value": "合并后的值"}}
  ]
}}
```

## 示例

### 示例1：完全不同的事实——无需变更（最常见情况）
已有属性：
p1: [hobby_activity] time=2023-05, value="2023-05-06，Andrew和朋友在蓝岭步道远足"
p2: [mood_event] time=2023-05, value="截至2023-05-03，Andrew被绿色环绕时感到平静"
新属性：
n1: [hobby_activity] time=2023-06, value="2023-06-11，Andrew和朋友参加了攀岩课程"
n2: [plan_event] time=2023-06, value="2023-06-13，Andrew计划尝试皮划艇"
输出：
```json
{{"existing": [], "new": []}}
```
原因：四条都描述不同的事件/事实。全部保留。

### 示例2：话题相似但事件不同——无需变更
已有属性：
p1: [hobby_activity] time=2023-05-06, value="2023-05-06，Andrew在蓝岭步道和朋友及女友远足"
新属性：
n1: [hobby_activity] time=2023-06-23, value="2023-06-23，Andrew和朋友远足，天气好，拍了很棒的照片"
输出：
```json
{{"existing": [], "new": []}}
```
原因：不同日期的两次远足，各有独特细节。都保留。

### 示例3：新值完全冗余——删除新值
已有属性：
p1: [hobby_activity] time=2023-06-05, value="2023-06-05，Andrew在蓝岭步道和朋友及女友远足并拍了很棒的照片"
新属性：
n1: [hobby_activity] time=2023-06, value="截至2023-06，Andrew最近去远足了"
输出：
```json
{{"existing": [], "new": [{{"id": "n1", "op": "delete"}}]}}
```
原因：n1 没有超出 p1 已有的任何信息。

### 示例4：计划已执行——删除旧计划
已有属性：
p1: [plan_event] time=2023-06, value="2023-06-13，Andrew计划尝试皮划艇"
新属性：
n1: [experience] time=2023-07, value="2023-07-05，Andrew和朋友在默里湖尝试了皮划艇并很喜欢"
输出：
```json
{{"existing": [{{"id": "p1", "op": "delete"}}], "new": []}}
```
原因：计划（p1）已执行——n1 用实际事件取代了它。p1 已过时。

### 示例5：演变中的状态——合并到已有属性
已有属性：
p1: [default_property] time=2023-06-02, value="截至2023-06-02，Andrew正在城市寻找宠物友好型公寓，已看了一些地方但没有成功"
新属性：
n1: [default_property] time=2023-08, value="截至2023-08，Andrew仍在寻找宠物友好型公寓，感到气馁但仍然坚定"
输出：
```json
{{"existing": [], "new": [{{"id": "n1", "op": "update", "target": "p1", "value": "从2023-06-02至2023-08，Andrew一直在城市寻找宠物友好型公寓，已看了一些地方但没有成功，虽然感到气馁但仍然坚定要找到合适的地方"}}]}}
```
原因：同一持续状态跨时间。合并保留完整时间线，避免重复。

### 示例6：新值为模糊旧属性补充上下文——更新已有属性
已有属性：
p1: [default_property] time=2023-03, value="上周（截至2023-03-27），Andrew从之前的工作经历了很大的变化"
新属性：
n1: [position_event] time=2023-03, value="上周（截至2023-03-27），Andrew开始了金融分析师的新工作"
输出：
```json
{{"existing": [{{"id": "p1", "op": "update", "value": "上周（截至2023-03-27），Andrew从之前的工作经历了很大的变化，开始了金融分析师的新职位"}}], "new": [{{"id": "n1", "op": "delete"}}]}}
```
原因：n1 提供了 p1 缺失的具体细节。合并到 p1 并跳过 n1。

只输出 JSON，不要包含额外文字。
"""
